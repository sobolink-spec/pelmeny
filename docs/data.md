# Формат данных — товары и категории 📦

Файл с данными: `src/data/products.js`.

## Товар (product)

Пример структуры:

```js
{
  id: 1,             // уникальный идентификатор (число)
  name: 'Название',  // строка
  category: 'Категория', // строка
  price: 149990,     // число (в копейках/рублях — здесь используется рубли)
  discount: 10,      // число — процент скидки (0 если нет)
  rating: 4.8,       // число (от 0 до 5)
  image: 'gamer-shop/img/id_1.png', // путь к изображению
  description: 'Текст', // описание
  specs: ['характеристика1', 'характеристика2'], // массив строк
  inStock: true      // булево значение
}
```

## Категории

Файл также экспортирует массив `categories` с объектами вида `{ id, name, value, count }`.

Использование данных:
- `Catalog` использует список продуктов для отображения карточек товаров
- `ProductDetail` / `ProductCard` получают один объект product и отображают детали

Если нужно, можно вынести данные в отдельный mock-API или подключить реальный бэкенд (fetch/axios).
