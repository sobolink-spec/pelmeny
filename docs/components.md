# Документация по компонентам — Gamer Shop 🧩

В этом файле описаны основные UI-компоненты приложения, их props и примеры использования.

---

## Header

**Расположение:** `src/components/Header/Header.jsx`

**Описание:** верхняя навигация с логотипом, ссылками на страницы и кнопкой для открытия корзины.

**Props:**
- `cartCount` (number) — количество товаров в корзине
- `onCartClick` (function) — обработчик клика по кнопке корзины

**Пример использования:**
```jsx
<Header cartCount={3} onCartClick={() => setCartOpen(true)} />
```

---

## Cart

**Расположение:** `src/components/Cart/Cart.jsx`

**Описание:** модальное окно с содержимым корзины, контролами количества, суммой и кнопкой оформления заказа.

**Props:**
- `isOpen` (boolean) — открыт ли компонент
- `onClose` (function) — закрыть корзину
- `items` (array) — массив объектов товаров в корзине: `{ id, name, price, quantity, image, category }`
- `onRemove` (function) — удалить товар по id
- `onUpdateQuantity` (function) — обновить количество (id, newQuantity)
- `total` (number) — общая сумма
- `onCheckout` (function) — обработчик оформления заказа

**Пример использования:**
```jsx
<Cart
  isOpen={cartOpen}
  onClose={() => setCartOpen(false)}
  items={cartItems}
  onRemove={removeFromCart}
  onUpdateQuantity={updateQuantity}
  total={cartTotal}
  onCheckout={handleCheckout}
/>
```

---

## Modal

**Расположение:** `src/components/Modal/Modal.jsx`

**Описание:** простой модальный контейнер для произвольного содержимого.

**Props:**
- `isOpen` (boolean) — открыть/закрыть
- `onClose` (function) — закрыть модал
- `children` (node) — содержимое

**Пример использования:**
```jsx
<Modal isOpen={showModal} onClose={() => setShowModal(false)}>
  <p>Какое-то содержимое</p>
</Modal>
```

---

## ProductCard (в `src/pages/ProductDetail/ProductDetail.jsx`)

**Описание:** карточка товара, отображает изображение, название, категорию, цену, рейтинг и кнопки "В корзину" и "Подробнее".

**Props:**
- `product` (object) — объект товара
- `addToCart` (function) — функция добавления в корзину (product)

**Пример использования:**
```jsx
<ProductCard product={product} addToCart={addToCart} />
```

---

## Footer

**Расположение:** `src/components/Footer/Footer.jsx`

**Описание:** статический футер с информацией о праве авторства.

**Props:** отсутствуют

---

Если хотите, могу дополнить документацию примерами пропсов с типизацией (PropTypes или TypeScript) и показать пример тестов для каждого компонента.
